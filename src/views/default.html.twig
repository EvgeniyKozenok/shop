<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <title> {% filter capitalize %}{{  title }}{% endfilter %} </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <!-- Bootstrap style -->
    <link id="callCss" rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" media="screen"/>
    <link href="/bootstrap/css/base.css" rel="stylesheet" media="screen"/>
    <!-- Bootstrap style responsive -->
    <link href="/bootstrap/css/bootstrap-responsive.min.css" rel="stylesheet"/>
    <link href="/bootstrap/css/font-awesome.css" rel="stylesheet" type="text/css">
    <!-- Google-code-prettify -->
    <link href="/bootstrap/js/google-code-prettify/prettify.css" rel="stylesheet"/>
    <link href=/css/default.css" rel="stylesheet" type="text/css">
    <!-- fav and touch icons -->
    <link rel="shortcut icon" href="/bootstrap/images/ico/favicon.ico">
    <link rel="apple-touch-icon-precomposed" sizes="144x144" href="/bootstrap/images/ico/apple-touch-icon-144-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="114x114" href="/bootstrap/images/ico/apple-touch-icon-114-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="72x72" href="/bootstrap/images/ico/apple-touch-icon-72-precomposed.png">
    <link rel="apple-touch-icon-precomposed" href="/bootstrap/images/ico/apple-touch-icon-57-precomposed.png">
    <style type="text/css" id="enject"></style>
</head>
<body>
<div id="header">
    <div class="container">
        <div id="welcomeLine" class="row">
            <div class="span6">Добрый день,<strong> {{ user.name }}</strong>!
            </div>
            <div class="span6">
                <div class="pull-right">
                    {% if user.id %}
                        <a href="{{ route('logout') }}" role="button" data-toggle="modal" style="padding-right:0">
                            <span class="btn btn-mini btn-danger">Выйти</span>
                        </a>
                    {% else %}
                    <a href="#login" role="button" data-toggle="modal" style="padding-right:0">
                        <span class="btn btn-mini btn-success btn-block">Войти</span>
                    </a>
                    {% endif %}
                    <br class="large">{% if error %}
                    <span class="btn-small btn-danger">{{ error }}</span>
                    {% endif %}
                    <div id="login" class="modal hide fade in" tabindex="-1" role="dialog" aria-labelledby="login" aria-hidden="false" >
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                            <h3>Авторизация</h3>
                        </div>
                        <div class="modal-body">
                            <form class="form-horizontal loginFrm" method="post" action="{{ route('login') }}">
                                <div class="control-group">
                                    <input type="text" id="inputEmail" name="email" placeholder="Email" value="{{ data.email }}">
                                </div>
                                <div class="control-group">
                                    <input type="password" id="inputPassword" name="password" placeholder="Пароль" value="{{ data.password }}">
                                </div>
                                <button type="submit" class="btn btn-success">Войти</button>
                            </form>
                            <a href="{{ route('check_in') }}" class="btn btn-default" value="{{ name }}">Зарегистрироваться</a>
                        </div>
                    </div>
                    <form action="{{ route('basket') }}" method="post">
                        <input class="hidden" id="basketData" name="id">
                        <button class="btn btn-mini btn-primary">
                            <i class="icon-shopping-cart icon-white"></i> <span id="countBasketProduct"></span> Продуктов в Вашей корзине
                        </button>
                        <span class="btn btn-mini btn-info" style="cursor: default"> <span id="basketSum"></span><sup>00</sup></span>
                        <span >&#8372</span>
                        <div id="clearBasket" class="btn btn-mini btn-danger">Очистить корзину</div>
                    </form>
                </div>
            </div>
        </div>
        <!-- Navbar ================================================== -->
        <div id="logoArea" class="navbar">
            <a id="smallScreen" data-target="#topMenu" data-toggle="collapse" class="btn btn-navbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
            <div class="navbar-inner">
                <a class="brand" href={{ route('root' ) }}><img src="???/???/logo.png" alt="интернет-магазин"/></a>
                <form class="form-inline navbar-search" method="post" >
                    <select class="srchTxt hidden">
                        <option name="all">все</option>
                        {% for category in categories %}
                        {% if category.nesting == false %}
                        <option name="{{ category.name }}">{% filter lower %}{{ category.description }}{% endfilter %}</option>
                        {% endif %}
                        {% endfor %}
                        {% for good in goods %}
                        {% if good.good_name%}
                        <option name="{{ good.good_name }}">{% filter lower %}{{ good.good_description }}{% endfilter %}</option>
                        {% endif %}
                        {% endfor %}
                    </select>
                    <input id="srchFld" class="srchTxt hidden" type="text" />
                    <button type="submit" id="submitButton" class="btn btn-primary hidden">Найти</button>
                </form>
                <ul id="topMenu" class="nav pull-right">
                    <li class="hidden">
                        <a href="special_offer.html">Скидки</a>
                    </li>
                    <li class="hidden">
                        <a href="normal.html">Доставка</a>
                    </li>
                    <li class="hidden">
                        <a href="contact.html">Контакты</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>
<!-- Header End====================================================================== -->

<div id="mainBody">
    <div class="container">
        <div class="row">
            <!-- Sidebar ================================================== -->
            <div id="sidebar" class="span3">
                <ul id="sideManu" class="nav nav-tabs nav-stacked">
                    {% for category in categories %}
                        {% if loop.first %}
                            {% if category.nesting %}
                            {% else %}
                                <li><a href="{{ route(category.name, ["id", 1]) }}" name="id={{ category.id }}"> {% filter upper %}{{ category.description }}{% endfilter %}</a></li>
                            {% endif %}
                        {% else %}
                            {% if category.nesting %}
                            {% else %}
                                <li><a href="{{ route(category.name) }}" name="id={{ category.id }}"> {% filter upper %}{{ category.description }}{% endfilter %}</a></li>
                            {% endif %}
                        {% endif %}
                    {% endfor %}
                </ul>
                {% block sidebar %}{% endblock %}
            </div>
            <!-- Sidebar end=============================================== -->
            {% block content %}{% endblock %}
        </div>
    </div>
</div>

<!-- Footer ================================================================== -->
<div  id="footerSection">
    <div class="container">
        <div class="row">
            <div class="span4">
                <h5>РАЗВИВАЮЩИЙСЯ ИНТЕРНЕТ МАГАЗИН</h5>
            </div>
            <div class="span4">
                <h5>АДРЕССА</h5>
                <p>г.СУМЫ, ул. РИМСКОГО_КОРСОКОВА, д.2<p>
            </div>
            <div class="span4">
                <h5>ТЕЛЕФОНЫ</h5>
                <p>+38 (095) 85-33-609</p>
                <p>+38 (066) 81-10-411</p>
                <p>+38 (096) 59-29-675</p>
            </div>
        </div>
        <p class="pull-right">&copy; author</p>
    </div><!-- Container End -->
</div>
<!-- Placed at the end of the document so the pages load faster ============================================= -->
<script src="/bootstrap/js/jquery.js" type="text/javascript"></script>
<script src="/bootstrap/js/bootstrap.min.js" type="text/javascript"></script>
<script src="/bootstrap/js/google-code-prettify/prettify.js"></script>

<script src="/bootstrap/js/bootshop.js"></script>
<script src="/bootstrap/js/jquery.lightbox-0.5.js"></script>
<script src="/js/compare.js"></script>
<script src="/js/sorter.js"></script>
<script src="/js/addToBasket.js"></script>
<script src="/js/modifyBasket.js"></script>

<!-- Themes switcher section ============================================================================================= -->
</body>
</html>